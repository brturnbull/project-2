<h1>photos index</h1>
  <% photos.forEach((photo) => { %>
    <div class="col s4 m7 l4">
      <div class="card s4 m7 l4">
        <div class="card-image">
          <img src="<%= photo.photourl %>">
        </div>
        <div class="card-action">
          <a href="/photos/<%=photo.id%>">view</a>
          <% if(photo.user) { %>
          <span class="card-title"><%= photo.user.username %></span>
          <% } %>
        </div>
      </div>
    </div>
  <% }) %>
</div>
